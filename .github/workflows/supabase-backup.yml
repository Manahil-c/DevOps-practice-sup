name: Supabase Backup

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # This will now run automatically every night at midnight

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: Install PostgreSQL 17 client
        run: |
          sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
          wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
          sudo apt-get update
          sudo apt-get install -y postgresql-client-17

      - name: Backup Supabase database
        env:
          # Use your secret name here (ensure it is set in GitHub Settings)
          PGPASSWORD: ${{ secrets.MSECRET }} 
          PGSSLMODE: require
        run: |
          pg_dump \
            -h aws-1-ap-southeast-1.pooler.supabase.com \
            -p 6543 \
            -U postgres.ksuxmhzpclzoterkltug \
            -d postgres \
            --format=custom \
            --no-owner \
            --no-privileges \
            -f supabase_backup.dump

      - name: Upload backup
        uses: actions/upload-artifact@v4
        with:
          name: supabase-backup
          path: supabase_backup.dump
          retention-days: 30 # Keeps your backups for 30 days